{
  "name": "packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "CI=1 lerna run build",
    "test": "CI=1 lerna run test"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
