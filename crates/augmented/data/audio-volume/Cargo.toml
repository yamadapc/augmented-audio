[package]
name = "audio-volume"
version = "0.1.0"
edition = "2018"
description = "Provides a data-type for volume"

[dependencies]

[features]
default = []
f64 = []

[dev-dependencies]
audio-processor-testing-helpers = { path = "../../testing/audio-processor-testing-helpers" , version = "1.0.0-alpha.2" }
