namespace augmented {
    void initialize_logger();
};

dictionary AvailableAudioOptions {
    sequence<string> host_ids;
    sequence<string> input_ids;
    sequence<string> output_ids;
};

dictionary AudioOptions {
    string? host_id;
    string? input_id;
};

interface AudioOptionsService {
    constructor();
    AvailableAudioOptions get_available_options();
    void set_options(AudioOptions model);
};

interface AudioEngineService {
    constructor();
    void start();
};
